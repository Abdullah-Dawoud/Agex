# Updates

Update checking is read-only:

```powershell
agex updates
agex updates -Json
```

Review official source, installed version, risk, and rollback before updating. Do not auto-update Codex, Node, removed Serena, MCP servers, memory tools, or routing tools. Back up Codex config before any intentional MCP change. Never reinstall Serena automatically.
