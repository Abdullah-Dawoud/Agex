<Project Sdk="Microsoft.NET.Sdk">

  <!-- Platform-independent AGEX engine: platform services, process runtime,
       agent adapters, orchestration, sessions, skills, settings and updates. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Agex.Core</RootNamespace>
    <AssemblyName>Agex.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <EmbeddedResource Include="Skills\skills-catalog.json" LogicalName="Agex.Core.Skills.skills-catalog.json" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Agex.Tests" />
  </ItemGroup>

</Project>
