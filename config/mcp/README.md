# MCP Configuration Guidance

Keep MCP configuration minimal and machine-specific. Do not commit live credentials or user state.

Before adding a server, document its problem, overlap, tools, permissions, credentials, maintenance source, disable path, and rollback plan in `docs/MCP.md` or a reviewed companion document.
