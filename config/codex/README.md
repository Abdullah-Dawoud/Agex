# Codex Configuration Guidance

Do not copy live Codex configuration into this repository. Keep auth, session, state, logs, databases, and machine-specific paths in the user profile.

Use this directory for safe documentation and future `.example` files only. Any future config change requires explicit backup, additive edit, validation, and plain `codex` continuity check.
